apiVersion: v1
kind: Service
metadata:
  name: {{ include "mystatefulset.fullname" . }}
  labels:
    {{- include "mystatefulset.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mystatefulset.selectorLabels" . | nindent 4 }}
